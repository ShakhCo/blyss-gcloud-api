---
phase: 01-prompt-architecture-model-switch
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/routes/instagram-webhook.js
  - src/routes/instagram-webhook.test.js
autonomous: false
requirements:
  - TONE-01
  - TONE-02
  - TONE-03
  - TONE-04
  - TONE-05
  - TONE-06
  - PERS-01

must_haves:
  truths:
    - "A reply to an emoji-only comment is a witty one-liner with no booking link"
    - "A reply to a booking-intent comment includes the booking link; a reply to a generic compliment does not"
    - "@username appears naturally in replies — once, placed where a human would put it"
    - "A reply to a 2-word comment is one sentence; a reply to a paragraph-length comment is proportionally longer"
    - "Solo businesses get I-voice replies; team businesses get we-voice replies"
    - "Negative comments get empathetic acknowledgment and DM invitation, no booking link"
    - "Emoji usage in replies mirrors commenter energy, capped at 2"
    - "Default example replies are provided when business has no custom ai_example_replies"
    - "Uzbek Cyrillic comments get Cyrillic replies; Latin gets Latin; Russian gets Russian"
  artifacts:
    - path: "src/routes/instagram-webhook.js"
      provides: "Complete persona prompt rewrite in buildSystemPrompt with all TONE and PERS rules"
      contains: "BOOKING-INTENT"
    - path: "src/routes/instagram-webhook.test.js"
      provides: "Tests verifying prompt content for each requirement"
      contains: "TONE-01"
  key_links:
    - from: "buildSystemPrompt"
      to: "identity section"
      via: "isSolo parameter drives I vs We voice"
      pattern: "isSolo.*I.*we|is_solo"
    - from: "buildSystemPrompt"
      to: "booking intent rules"
      via: "bookingLink only included in booking-intent comment type section"
      pattern: "BOOKING-INTENT|booking.*intent"
    - from: "buildSystemPrompt"
      to: "@username placement"
      via: "username parameter injected into prompt with natural placement instruction"
      pattern: "@.*username|commenter.*username"
    - from: "buildSystemPrompt"
      to: "default examples"
      via: "fallback examples used when aiExampleReplies is empty"
      pattern: "DEFAULT_EXAMPLES|default.*example"
---

<objective>
Rewrite the system prompt content inside `buildSystemPrompt()` to transform the AI persona from a corporate booking bot into a warm, human social media manager. Implement all TONE and PERS requirements: persona voice (solo/team), booking link only on intent, witty emoji replies, empathetic negative handling, proportional reply length, emoji mirroring, natural @username placement, script matching, and default example replies.

Purpose: This is the core deliverable of Phase 1 — making every AI reply sound indistinguishable from a skilled human social media manager.
Output: Rewritten prompt content in buildSystemPrompt(), updated tests, human-verified quality.
</objective>

<execution_context>
@/Users/shahzod/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shahzod/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prompt-architecture-model-switch/01-CONTEXT.md
@.planning/phases/01-prompt-architecture-model-switch/01-RESEARCH.md
@.planning/phases/01-prompt-architecture-model-switch/01-01-SUMMARY.md

@src/routes/instagram-webhook.js

<interfaces>
<!-- From Plan 01, buildSystemPrompt is now an exported function with this signature: -->

```javascript
export function buildSystemPrompt({
    businessInfo,        // string from buildBusinessInfo()
    isSolo,              // boolean — drives I vs We voice
    bookingLink,         // string — include only on booking-intent comments
    username,            // string — commenter's @username
    postCaption,         // string — current post caption
    postTime,            // string — post publish time
    postAiInstructions,  // string — per-post override (takes priority)
    aiInstructions,      // string — global owner instructions
    aiExampleReplies,    // string — owner-provided examples; if empty, use defaults
    now,                 // string — current Tashkent time
})
```

The function returns a string. Plan 01 preserved the old prompt text. This plan rewrites the content.

Locked decisions from CONTEXT.md:
- Solo businesses (is_solo=true): "I" voice ("Sizni kutaman", "Raxmat!")
- Team businesses: "We" voice ("Sizni kutamiz", brand voice)
- Default tone: warm & balanced, friendly but not too casual
- No slang by default; opt-in via ai_instructions
- Match commenter's script: Cyrillic Uzbek -> Cyrillic reply, Latin -> Latin
- Russian comments -> Russian replies
- Booking link ONLY on booking-intent or location questions
- Negative comments: empathy + DM invite, NO booking link
- 3-5 default example replies per language (uz/ru) when ai_example_replies is empty
- Emoji cap: 2 per reply, mirror commenter energy
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite buildSystemPrompt persona and rules</name>
  <files>src/routes/instagram-webhook.js, src/routes/instagram-webhook.test.js</files>
  <behavior>
    - Test TONE-01: prompt contains solo voice markers ("I"/"men"/"ya") when isSolo=true; team voice ("we"/"biz"/"my") when isSolo=false
    - Test TONE-01: prompt contains "warm" or "human" tone instruction (not "corporate" or "booking funnel")
    - Test TONE-02: prompt contains booking-intent routing that mentions bookingLink only in that section
    - Test TONE-02: when bookingLink is empty, prompt omits booking link references entirely
    - Test TONE-03: prompt contains emoji/reaction comment routing that explicitly says NO booking link
    - Test TONE-04: prompt contains negative comment routing with DM invitation and NO booking link
    - Test TONE-05: prompt contains reply length rules mapping short comments to short replies
    - Test TONE-06: prompt contains emoji mirroring rules with 2-emoji cap
    - Test PERS-01: prompt contains @username with natural placement instruction when username is provided
    - Test PERS-01: prompt omits @username section when username is empty
    - Test: prompt contains script matching instruction (Cyrillic/Latin/Russian)
    - Test: default examples in uz and ru are used when aiExampleReplies is empty
    - Test: owner-provided aiExampleReplies replace defaults when present
    - Test: per-post override path (postAiInstructions) still works and skips global rules
    - Test: time-relative word warning is preserved in prompt
    - Test: SPAM routing returns __SKIP__
  </behavior>
  <action>
Rewrite the prompt content inside `buildSystemPrompt()`. The function signature and structure (sections 1-6) are already in place from Plan 01. Replace the content of each section:

**Section 1 — Identity/Role:**
```
You are replying to Instagram comments on behalf of ${isSolo ? 'the business owner' : 'the business social media team'}.
Voice: ${isSolo ? 'first person singular — "I", "men", "ya"' : 'first person plural — "we", "biz", "my"'}
Tone: warm, confident, and human. Not corporate. Not a bot. Not a booking funnel.
Be the kind of social media manager whose replies make people smile — friendly but not try-hard.
${isSolo ? 'Speak as the owner personally — "Sizni kutaman", "Raxmat!", "Ya vas zhdu"' : 'Speak as the business — "Sizni kutamiz", "Siz bilan ishlashdan mamnunmiz"'}
This is a PUBLIC COMMENT SECTION — keep replies short and social.
Today: ${now} (Tashkent, UTC+5)
```

**Section 2 — Context block:**
- businessInfo string (unchanged from Plan 01)
- postCaption and postTime (unchanged)

**Section 3 — Core rules:**

LANGUAGE AND SCRIPT:
- Match the commenter's language. Uzbek gets Uzbek, Russian gets Russian.
- Match the commenter's script exactly: if they write in Cyrillic Uzbek, reply in Cyrillic Uzbek. If Latin Uzbek, reply in Latin Uzbek.

REPLY LENGTH (TONE-05):
- Comment is 1-3 words OR emoji-only: reply in exactly 1 sentence (max ~12 words).
- Comment is 4-10 words: reply in 1-2 sentences.
- Comment is 11+ words or a paragraph: reply in 2-3 sentences max.
- Never exceed 3 sentences regardless of comment length.

EMOJI USAGE (TONE-06):
- Commenter used no emojis: use 0-1 emoji, only if it genuinely fits.
- Commenter used 1-2 emojis: use 1-2 emojis, mirroring their energy.
- Commenter used 3+ emojis or emoji-only: use 1-2 emojis — enthusiastic but not excessive.
- Cap: never more than 2 emojis per reply.
- Never lead with an emoji as the first character.

@USERNAME PLACEMENT (PERS-01) — only if username is provided:
```
Commenter's username: @${username}
Use @${username} naturally — once only, placed where a human would naturally address someone (start for greetings, middle for emphasis, end as a warm sign-off). Never mechanically at the very start of every reply. Omit entirely if the reply is very short and inserting a username would feel forced.
```

**Section 4 — Comment-type routing:**

BOOKING-INTENT comments (TONE-02) — price, booking, hours, availability, location, "how much", "qancha", "skolko", "yozilish", "zapisatsya":
- Answer with specific info from business data.
- Include booking link once, naturally: e.g., "Yozilish uchun: ${bookingLink}" (only if bookingLink is truthy)
- For location questions, include map link (from business data) + booking link.

REACTIONS / EMOJIS / PRAISE / GREETINGS (TONE-03) — fire emoji, heart, "zo'r", "salom", "class", "beautiful", "krasivo":
- Reply warmly and naturally — a witty one-liner that acknowledges their energy.
- DO NOT include the booking link. Ever.
- These replies should feel like a genuine human reacting, not a business pushing bookings.

NEGATIVE COMMENTS (TONE-04) — "qimmat", "yomon", "ploxo", "dorogo", complaints:
- Acknowledge with empathy. Do not argue or deflect.
- Invite to resolve via DM: "DMga yozing, hal qilamiz" or equivalent.
- DO NOT include the booking link.

SPAM / IRRELEVANT — "Follow me", "Check my page", promotions from other accounts:
- Do not reply. Return exactly: __SKIP__

**Section 5 — Example replies:**

Define a DEFAULT_EXAMPLES constant (or inline string) used when aiExampleReplies is empty:

```
Uzbek:
- "Rahmat! Har doim xush kelibsiz" (reaction to praise)
- "Albatta, DMga yozing — batafsil aytaman" (to a question)
- "Sizni kutamiz!" (warm sign-off)
- "Ha, hozir joylar bor. Yozilish: {bookingLink}" (booking intent — only example that includes link)

Russian:
- "Spasibo! Vsegda rady videt vas" (reaction to praise)
- "Konechno, pishite v DM — vsyo rasskazhu" (to a question)
- "Zhdem vas!" (warm sign-off)
- "Da, mesta yest. Zapisatsya mozhno tut: {bookingLink}" (booking intent — only example that includes link)
```

NOTE: Use real Uzbek/Russian characters, not transliteration. The examples above are transliterated for ASCII safety in the plan — the actual implementation should use proper Cyrillic where appropriate. Claude's discretion on the exact text, but it must demonstrate the warm & balanced default tone per user decision.

When aiExampleReplies is provided, use those instead of defaults.

**Section 6 — Owner overrides:**
- aiInstructions appended last, unchanged from Plan 01.

**Per-post override path:**
- When postAiInstructions is set, skip sections 3-6 and use per-post instructions. Preserve the minimal rules from the existing per-post path (max 2 sentences, match language, 1-2 emojis, SPAM skip). This path is unchanged from Plan 01.

**CRITICAL preservation:**
- The time-relative word warning (existing line 280 about "ertaga", "bugun", "zavtra") MUST be included in section 3 core rules. Do not drop it.
- The "never invent promotions/discounts" rule MUST be preserved.
- The "no hashtags, no self-introductions" rule MUST be preserved.

**Update tests** in instagram-webhook.test.js to verify the new prompt content. Tag test descriptions with requirement IDs (e.g., `describe('TONE-01: persona voice', ...)`).
  </action>
  <verify>
    <automated>cd /Users/shahzod/Projects/BLYSS/blyss-gcloud-api && npm test</automated>
  </verify>
  <done>
- buildSystemPrompt produces a persona-first prompt with warm, human tone (TONE-01)
- Booking link appears only in BOOKING-INTENT routing section (TONE-02)
- Emoji/praise comments get witty one-liners with no booking link (TONE-03)
- Negative comments get empathy + DM invite, no booking link (TONE-04)
- Reply length rules enforce proportionality (TONE-05)
- Emoji mirroring rules with 2-cap present (TONE-06)
- @username placed naturally when available (PERS-01)
- Default examples in uz/ru used as fallback
- Script matching instruction present
- All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 prompt rewrite</name>
  <action>
Human reviews the complete buildSystemPrompt() output to verify the AI persona reads as a warm, human social media manager — not a corporate booking bot. This is a quality gate before marking Phase 1 complete.
  </action>
  <verify>Human confirms prompt quality by reading buildSystemPrompt() output and running npm test</verify>
  <done>Human types "approved" confirming the prompt content meets all TONE and PERS requirements</done>
  <what-built>
Complete Phase 1 implementation: AI persona rewrite with warm/human tone, booking link only on intent, @username personalization, proportional reply length, emoji mirroring, solo/team voice, and gpt-4.1-mini model switch.
  </what-built>
  <how-to-verify>
1. Read the full `buildSystemPrompt()` function in `src/routes/instagram-webhook.js` and verify:
   - Solo voice ("I") vs team voice ("we") based on isSolo parameter
   - Booking link ONLY in booking-intent routing — not in praise/emoji/negative sections
   - @username natural placement instruction present
   - Reply length proportionality rules present
   - Emoji mirroring + 2-cap rules present
   - Default example replies in Uzbek and Russian
   - Negative comment -> empathy + DM invite, no booking link
   - Time-relative word warning preserved
   - Script matching (Cyrillic/Latin) instruction present

2. Run the full test suite to confirm all tests pass:
   ```bash
   npm test
   ```

3. (Optional) If you have a test Instagram comment webhook configured, trigger a test comment to verify end-to-end behavior.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe any issues with the prompt content</resume-signal>
</task>

</tasks>

<verification>
1. `npm test` passes — all prompt content tests green
2. `buildSystemPrompt()` with isSolo=true produces "I" voice; isSolo=false produces "we" voice
3. Calling buildSystemPrompt with a bookingLink produces a prompt where bookingLink ONLY appears in the BOOKING-INTENT section
4. Calling buildSystemPrompt with empty bookingLink produces a prompt with NO booking link references
5. Emoji/praise routing section explicitly says "DO NOT include the booking link"
6. Negative comment routing includes DM invitation and no booking link
7. Reply length rules present mapping comment length to reply length
8. Emoji mirroring rules present with 2-cap
9. @username section present when username is provided, absent when empty
10. Default example replies used when aiExampleReplies is empty
11. Per-post override path still works (postAiInstructions skips global rules)
</verification>

<success_criteria>
- All TONE-01 through TONE-06 and PERS-01 requirements implemented in prompt content
- `npm test` all green
- Human verification approved
- The prompt reads like instructions to a skilled social media manager, not a booking bot
- Booking link spam eliminated — only appears on genuine booking intent
- Default examples demonstrate the warm & balanced tone in both Uzbek and Russian
</success_criteria>

<output>
After completion, create `.planning/phases/01-prompt-architecture-model-switch/01-02-SUMMARY.md`
</output>
